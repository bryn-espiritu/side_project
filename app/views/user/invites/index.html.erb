<%= render "layouts/notification_messages" %>
<div class="container">
  <div class="border-3 mb-5 d-flex justify-content-center"> <%= @qrcode if user_signed_in? %></div>
  <div class="text-center alert alert-info w-75 container"><%= @invite_link if user_signed_in? %></div>
</div>

